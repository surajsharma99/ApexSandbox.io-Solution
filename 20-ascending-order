public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    if(a>b && b>c && a>c)
    {
        return true;
    }
    else if(a == b && a>c && b>c)
    {
        return true;
    }
    else
    {
        return false;
    }
}