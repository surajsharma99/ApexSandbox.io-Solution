public Boolean isEndWithZero(Integer num){
    for(Integer i = 0 ; i < num.length() ; i++)
    {
        if(i == num.length()-1 && num.length()-1 == 0)
        {
            return true;
        }
        else
            return false;
    }
    return;
}